Deadline Aman<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DIGICORE | Joki Tugas Berbasis AI</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* --- Bagian CSS --- */
        :root {
            --primary-color: #4a00e0; /* Ungu gelap */
            --secondary-color: #8e2de2; /* Ungu terang */
            --accent-color: #00bcd4; /* Cyan terang */
            --text-color: #e0e0e0;
            --background-gradient: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background: var(--background-gradient);
            color: var(--text-color);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            text-align: center;
            overflow-x: hidden;
            position: relative;
        }

        /* Efek Latar Belakang */
        .shape {
            position: absolute;
            filter: blur(80px);
            opacity: 0.3;
            border-radius: 50%;
            z-index: 0;
        }

        .shape-1 { top: 10%; left: 10%; width: 200px; height: 200px; background-color: var(--accent-color); animation: moveShape 15s infinite alternate; }
        .shape-2 { bottom: 5%; right: 5%; width: 300px; height: 300px; background-color: var(--secondary-color); animation: moveShape 20s infinite alternate-reverse; }

        @keyframes moveShape {
            from { transform: translate(0, 0) scale(1); }
            to { transform: translate(50px, 50px) scale(1.1); }
        }

        .container {
            z-index: 10;
            padding: 40px 20px;
            max-width: 90%;
            border-radius: 15px;
            background: rgba(0, 0, 0, 0.3); /* Transparan gelap */
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.18);
        }

        h1 {
            font-size: 3em;
            margin-bottom: 10px;
            font-weight: 800;
            letter-spacing: 2px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        /* Efek Ketikan */
        #typing-text {
            color: var(--accent-color);
            font-size: 1.5em;
            font-weight: 600;
            margin-bottom: 30px;
            height: 1.5em; /* Memastikan ruang tetap ada */
            white-space: nowrap;
            overflow: hidden;
            border-right: .15em solid var(--accent-color); /* Cursor ketikan */
            animation: blink-caret .75s step-end infinite;
        }

        @keyframes blink-caret {
            from, to { border-color: transparent }
            50% { border-color: var(--accent-color); }
        }

        .cta-buttons {
            display: flex;
            flex-direction: column;
            gap: 20px;
            margin-top: 30px;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 15px 30px;
            border-radius: 50px;
            font-size: 1.2em;
            font-weight: 700;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 300px;
            margin: 0 auto;
        }

        /* Tombol WhatsApp */
        .btn-wa {
            background: linear-gradient(45deg, #128C7E, #25D366);
            color: white;
            border: none;
        }

        .btn-wa:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(37, 211, 102, 0.5);
        }

        /* Tombol Instagram */
        .btn-ig {
            background: linear-gradient(45deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888);
            color: white;
            border: none;
        }

        .btn-ig:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(230, 104, 60, 0.5);
        }

        .btn i {
            margin-right: 10px;
        }
        
        /* Gambar Ilustrasi (Opsional: Hanya untuk visualisasi) */
        .illustration img {
            max-width: 250px;
            margin-bottom: 20px;
            filter: drop-shadow(0 0 10px rgba(0, 255, 255, 0.5)); /* Efek cahaya neon */
        }

        .services {
            margin-top: 40px;
            font-size: 1.1em;
        }
        .services span {
            display: block;
            margin: 5px 0;
            font-weight: 300;
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;600;800&display=swap" rel="stylesheet">
</head>
<body>
    <div class="shape shape-1"></div>
    <div class="shape shape-2"></div>

    <div class="container">
        <div class="illustration">
            <img src="https://via.placeholder.com/150/00bcd4/ffffff?text=DIGICORE" alt="Logo DIGICORE">
        </div>

        <h1>DIGICORE JOKI TUGAS</h1>
        <div id="typing-text"></div>

        <p class="services">
            ✅ Layanan Tugas untuk SMP, SMA, dan KULIAH (Makalah, PPT, Dll.)<br>
            🧠 Didukung AI untuk Riset Cepat & Hasil Akurat!<br>
            💯 Garansi Deadline Aman & Anti Plagiarisme!
        </p>

        <div class="cta-buttons">
            <a href="https://wa.me/6282181642280?text=Halo%2C%20saya%20mau%20joki%20tugas%20untuk%20level%20(SMP%2FSMU%2FKuliah).%20Bisa%20bantu%3F" class="btn btn-wa" target="_blank">
                <i class="fab fa-whatsapp"></i> ORDER VIA WHATSAPP (FAST RESPON)
            </a>

            <a href="https://www.instagram.com/_digicore" class="btn btn-ig" target="_blank">
                <i class="fab fa-instagram"></i> CEK PORTOFOLIO DI INSTAGRAM
            </a>
        </div>
    </div>

    <script>
        const textElement = document.getElementById('typing-text');
        const phrases = [
            "Solusi Cepat Tugas Sekolah.",
            "Solusi Tuntas Tugas Kuliah.",
            "Tugas Beres, Deadline Aman.",
            "Didukung Teknologi AI Canggih."
        ];
        let phraseIndex = 0;
        let charIndex = 0;
        let isDeleting = false;

        function type() {
            const currentPhrase = phrases[phraseIndex];

            if (isDeleting) {
                // Menghapus teks
                textElement.textContent = currentPhrase.substring(0, charIndex - 1);
                charIndex--;
            } else {
                // Mengetik teks
                textElement.textContent = currentPhrase.substring(0, charIndex + 1);
                charIndex++;
            }

            // Perhitungan kecepatan
            let typingSpeed = isDeleting ? 50 : 100;

            if (!isDeleting && charIndex === currentPhrase.length) {
                // Selesai mengetik, tunggu sebentar sebelum menghapus
                typingSpeed = 1500;
                isDeleting = true;
            } else if (isDeleting && charIndex === 0) {
                // Selesai menghapus, pindah ke frasa berikutnya
                isDeleting = false;
                phraseIndex = (phraseIndex + 1) % phrases.length;
                typingSpeed = 500;
            }

            setTimeout(type, typingSpeed);
        }

        // Mulai efek ketikan saat web dimuat
        document.addEventListener('DOMContentLoaded', () => {
            setTimeout(type, 500);
        });
    </script>
</body>
</html> & Anti Plagiarisme!